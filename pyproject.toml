[project]
name = "marks"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.0",
  "pandas>=2.0",
  "duckdb>=0.10",
  "PyYAML>=6.0",
  "anthropic>=0.40",
]

[tool.vercel]
entrypoint = "api/index.py"

